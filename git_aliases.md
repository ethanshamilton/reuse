- epush
```
git config --global alias.epush '!GIT_SSH_COMMAND="ssh -i ~/.ssh/798" git -c user.name="ethanshamilton" -c user.email="ethan.hamilton798@gmail.com" push'
```

- epull
```
git config --global alias.epull '!GIT_SSH_COMMAND="ssh -i ~/.ssh/798" git -c user.name="ethanshamilton" -c user.email="ethan.hamilton798@gmail.com" pull'
```

- emerge
```
git config --global alias.emerge '!GIT_SSH_COMMAND="ssh -i ~/.ssh/798" git -c user.name="ethanshamilton" -c user.email="ethan.hamilton798@gmail.com" merge'
```

- ecommit
```
git config --global alias.ecommit '!GIT_SSH_COMMAND="ssh -i ~/.ssh/798" git -c user.name="ethanshamilton" -c user.email="ethan.hamilton798@gmail.com" commit'
```

- eclone
```
git config --global alias.eclone '!GIT_SSH_COMMAND="ssh -i ~/.ssh/798" git -c user.name="ethanshamilton' -c user.email="ethan.hamilton798@gmail.com" clone'
```
